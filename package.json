{
  "name": "fin",
  "private": true,
  "author": "Travis Fischer <travis@transitivebullsh.it>",
  "repository": "functional-income/fin",
  "license": "MIT",
  "engines": {
    "node": ">=8"
  },
  "keywords": [
    "faas",
    "saas",
    "lambda",
    "serverless",
    "deploy",
    "fin",
    "fts"
  ],
  "scripts": {
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish",
    "preinstall": "node -e \"if (process.env.npm_execpath.indexOf('yarn') < 0) throw new Error('fin requires yarn for development')\"",
    "postinstall": "run-s bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.13.4",
    "npm-run-all": "^4.1.5"
  }
}
