curl -X POST{{#hasFileOutput}} -o '{{output}}'{{/hasFileOutput}} \
  '{{{service.url}}}' \
  -H 'content-type: application/json' \
{{#token}}  -H 'authorization: {{token}}' \
{{/token}}  -d '{{{exampleJSONPretty}}}'
